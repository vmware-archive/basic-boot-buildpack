#!/usr/bin/env bash

set -e

pwd
ls
echo "Running agent..."
/vendor/bin/java -jar secret-agent.jar &

cat << EOF
addons: []
config_vars: []
default_process_types:
  web: vendor/bin/java -jar application.jar
EOF