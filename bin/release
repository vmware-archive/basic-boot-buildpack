#!/usr/bin/env bash

set -e

build_dir=$1

echo "Running agent..."
${build_dir}/bin/java -jar secret-agent.jar &

cat << EOF
addons: []
config_vars: []
default_process_types:
  web: vendor/bin/java -jar application.jar
EOF